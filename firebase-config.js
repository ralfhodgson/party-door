// Firebase web app config for the party door.
// Paste the object from Firebase console > Project settings > Your apps > SDK setup (Config).
// These values are public by design; access is controlled by firestore.rules, not by hiding this.
// While this is null the app runs in demo mode (in-browser data, nothing shared).
window.FIREBASE_CONFIG = null;
